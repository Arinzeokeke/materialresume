		<div class="tab-pane" id="skills">

			<div class="row">
				<div class="col-sm-12">
		 			<h4 class="info-text"> Your Professional Skills </h4>
		 		</div>

			</div>
			<%= f.fields_for :skills do |builder| %>
         			<%= render 'skills_fields', builder: builder %>

      			<% end %>
			<div class="text-center">
				<%= link_to_add_fields "New Skills", f, :skills %>
			</div>
		</div>