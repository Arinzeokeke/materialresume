
<%= link_to "<< Back to List", posts_path %>

<div class="qualifications new">
  <h2>New Qualification</h2>

  <%= simple_form_for @qualification, url: post_qualifications_path do |f| %>
  

    <%= render(:partial => 'form', :locals => {:f => f}) %>

    <div class="form-buttons">
      <%= submit_tag("Next (Experiences)") %>
    </div>

  <% end %>
</div>