<%= link_to "<< Back to List", posts_path %>

<div class="post edit">
  <h2>Update Experiences</h2>

  <%= simple_form_for @experience, url: post_experience_path(@post.id, @experience.id) do |f| %>
  

    <%= render(:partial => 'form', :locals => {:f => f}) %>

    <div class="form-buttons">
      <%= submit_tag("Next (Projects)") %>
    </div>

  <% end %>
</div>